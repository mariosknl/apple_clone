# add the linters you want stickler to use for this project
linters:
  stylelint:
    config: 'stylelint.config.js'

    files:
      ignore:
        - './images/*.jpg'
        - './images/*.png'
        - '*.png'
        - '*.jpg'
